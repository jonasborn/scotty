<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="navbar-toggler-icon"></span>
    </button>
    <a class="navbar-brand" href="#">FN Scotty</a>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">


        <ul class="navbar-nav">
            {% for entry in available %}
                {% if entry.title == current %}
                    <li class="nav-item active">
                        <a class="nav-link" href=".">{{ entry.title }}<span class="sr-only">(current)</span></a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ entry.url }}">{{ entry.title }}</a>
                    </li>
                {% endif %}
            {% endfor %}
        </ul>

        <ul class="navbar-nav ml-md-auto">
            <a class="nav-link" href="#">Über & Impressum</a>
        </ul>
    </div>
</nav>